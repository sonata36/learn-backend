package com.learn.learnbackend.auth;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * 阶段 3 版本一：签名 Cookie 测试（暂缓实现）。
 * 待覆盖：登录后携带 Cookie 访问受保护接口成功、篡改 Cookie 被拒、退出后失效、过期失效。
 */
@Disabled("阶段 3 实现签名 Cookie 后启用")
class CookieAuthTest {

    @Test
    void placeholder() {
        // TODO 阶段 3
    }
}
