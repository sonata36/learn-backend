package com.learn.learnbackend.auth;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * 阶段 3 版本二：Cookie + Session 测试（暂缓实现）。
 * 待覆盖：登录建立 Session、受保护接口通过、Session 不存在/过期/退出后复用返回 401。
 */
@Disabled("阶段 3 实现 Cookie + Session 后启用")
class SessionAuthTest {

    @Test
    void placeholder() {
        // TODO 阶段 3
    }
}
